pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "moko-resources-sample"

include(":androidApp")
include(":shared")
